apiVersion: apps/v1
kind: Deployment
metadata:
  name: capstone-project
  labels:
    app: capstone-project
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: capstone-projects
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: capstone-project
    spec:
      containers:
        - image: kevmare/capstone_project
          imagePullPolicy: Always
          name: capstone-project-cloud-devops
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: kevmare/capstone_project
  labels:
    app: kevmare/capstone_project
spec:
  type: LoadBalancer
  selector:
    app: kevmare/capstone_project
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30000
      protocol: TCP