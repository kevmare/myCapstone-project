apiVersion: apps/v1
kind: Deployment
metadata:
  name: capstone_project
  labels:
    app: capstone_project
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: capstone_project
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: capstone_project
    spec:
      containers:
        - image: kevmare/capstone_project
          imagePullPolicy: Always
          name: capstone_project
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: capstone_project
  labels:
    app: capstone_project
spec:
  type: LoadBalancer
  selector:
    app: capstone_project
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30000
      protocol: TCP